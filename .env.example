## Frontend environment variables (example)
# Vite variables must be prefixed with VITE_
# API base URL used by the app (local worker default)
VITE_API_URL=http://localhost:8787

# Optional: override build-time base path
# VITE_BASE_PATH=/
